# 돌봄로봇 주간 업무보고 웹앱 — 인수인계 문서

## 무엇을 하는 앱인가

- **목적**: 매주 팀원 10명이 주간 업무보고를 웹으로 입력 → 누구나 HWPX 취합본을 다운로드해서
  주간회의에서 띄움. 여기에 팀 협업 기능(공지·할 일·요청·일정·구매·장비 등)이 붙어
  **Dolbom Studio**라는 팀 업무 공간이 됨.
- **사용자**: 연구원 10명 (+ 간부 3명은 이름 명단에만 있음 — `EXTRA_USER_NAMES`)
- **접속 URL**: https://dolbom-studio.streamlit.app
  (구 주소 `carerobot-weekly-report.streamlit.app` — 레포 이름은 그대로라 헷갈리기 쉬움)
- **로그인**: **개인 계정**(2026-07 전환). 회원가입 → 관리자 승인 → 로그인.
  비밀번호는 pbkdf2-sha256 해시로만 저장(`app/account_store.py`, 구글시트 `계정` 탭).
  자동 로그인은 `?uid=&tok=` + localStorage(`streamlit-js-eval`).
  - 공용 비밀번호(`APP_PASSWORD`)는 **옛 흔적** — 지금 로그인에 쓰지 않음.
  - `is_admin`은 `ADMIN_IDS`(현재 `admin`)로 판정하며 **`👤 회원 관리` 메뉴에만** 쓰임.
    그 외 기능(공지·백업·취합본 생성·구매 완료처리 등)은 전원 사용.

## 기술 스택 및 구조

```
┌──────────────────────────────────────────┐
│ Streamlit Cloud (streamlit.app)          │ ← 앱 호스팅
│   └─ GitHub(carerobot0001-pixel/          │
│         carerobot-weekly-report)에서 자동 │
├──────────────────────────────────────────┤
│ Google Sheets ("돌봄로봇_업무보고_제출함")  │ ← 입력 데이터 저장
│   └─ 서비스 계정(streamlit-bot@…)으로 접근 │
├──────────────────────────────────────────┤
│ HWPX 템플릿 (레포 루트의 *.hwpx 파일들)    │ ← 취합본 생성용
└──────────────────────────────────────────┘
```

## 파일 구조

```
돌봄로봇_업무보고_김건양/                    ← 레포 루트
├── CLAUDE.md                               ← 이 문서
├── .gitignore                              ← 비밀파일 차단 규칙
├── 돌봄로봇_업무보고(*.hwpx)                ← HWPX 템플릿들 (과거 주차들)
└── app/                                    ← Streamlit 앱
    ├── streamlit_app.py                    ← 메인 앱 (UI, 라우팅)
    ├── team_config.py                      ← 팀원 10명 + 셀 매핑 + 비밀번호
    ├── sheets_store.py                     ← 구글시트 읽기/쓰기 (업무보고 제출함)
    ├── space_store.py                      ← 스마트돌봄스페이스 외부 시트 연동 (FAQ/관리대장)
    ├── purchase_store.py                   ← 구매요청 시트 누적 + 엑셀 양식 생성
    ├── collab_store.py                     ← 문서 협업 보드 (구글문서 링크+요청+현황)
    ├── equip_store.py                      ← 장비 사용현황 대장 (연구별 필터+편집)
    ├── visit_store.py                      ← 실증 방문 일지 (등록/필터/삭제)
    ├── calendar_store.py                   ← 사업단 구글캘린더 연동 (조회/추가/수정/삭제)
    ├── news_store.py                       ← 홈 뉴스피드 (구글뉴스 RSS)
    ├── notice_store.py                     ← 홈 팀 공지사항 (누구나 등록/삭제)
    ├── todo_store.py                        ← 홈 '내 할 일'(업무·완료기록) + 팀 공용 설정 저장
    │                                          (개인 할 일은 시트 아닌 브라우저 저장)
    ├── request_store.py                     ← 팀 할 일 '요청' 배정 (요청·회신·완료·회수)
    ├── feedback_store.py                    ← 앱 개선 요청 (오류·개선 의견 수집)
    ├── mail_store.py                        ← CC 수신 메일 수집(Apps Script) + 키워드 분류
    ├── account_store.py                     ← 개인 계정(가입·승인·로그인, pbkdf2 해시)
    ├── calendar_image.py                    ← 취합본 마지막 장 월간 달력 BMP 생성
    ├── resource_store.py                    ← 자료실 (팀 공용 참고자료·양식 링크 보드)
    ├── common_store.py                     ← 사업단 공통확인사항(최혜민) 표→한글/엑셀
    ├── hwpx_exporter.py                    ← HWPX 취합본 생성
    ├── requirements.txt                    ← 파이썬 의존성
    ├── SETUP.md                            ← 초기 설치 가이드
    ├── .streamlit/
    │   ├── secrets.toml                    ← [비밀] 구글 서비스 계정 키 등
    │   └── secrets.toml.example            ← 예시 템플릿
    └── service_account.json                ← [비밀] 구글 서비스 계정 원본 JSON
```

## 핵심 개념: 셀 매핑 (`team_config.py`)

각 팀원이 HWPX 어느 셀에 내용이 들어갈지 `cells` 딕셔너리로 정의. 형식:

```python
"cells": {
    "필드명": (col, row),             # 기본 검정색, 첫 매치
    "필드명": (col, row, "blue"),      # 파란색
    "필드명": (col, row, "black", 1),  # nth번째 매치 (중복 주소 처리)
}
```

**필드 종류**: `acquired_data`(파란색), `research_done/plan`, `task_done/plan`, `smart_care_space`(백정은), `project_confirmation`(최혜민, Table 1), `research_meeting/director_meeting/mohw_weekly`(최혜민 회의자료).

## 자주 하는 작업

### 팀원 추가/삭제/순서 변경
`app/team_config.py`의 `TEAM_MEMBERS` 리스트 수정 → git push → Streamlit Cloud 자동 재배포.
**주의**: 셀 매핑(`col, row`)은 HWPX 템플릿 구조에 의존. 팀원 추가 시 먼저 템플릿에 행 추가 필요.

### HWPX 템플릿 구조 변경
1. 한글에서 템플릿 편집 → 새 `.hwpx` 파일로 저장 (레포 루트에)
2. 셀 구조 분석:
   ```bash
   cd app && python -c "
   import zipfile, re
   with zipfile.ZipFile('../새템플릿.hwpx') as z:
       xml = z.read('Contents/section0.xml').decode('utf-8')
   # 메인 테이블 셀들 나열
   for m in re.finditer(r'cellAddr colAddr=\"(\d+)\" rowAddr=\"(\d+)\"', xml):
       print(m.group())
   "
   ```
3. `team_config.py`의 `cells` 좌표 수정
4. 기존 HWPX로 테스트 후 push

### 계정·비밀번호 (2026-07 개인 계정으로 전환됨)
- 가입 승인/거부: **`👤 회원 관리`** 메뉴(관리자 아이디 = `team_config.ADMIN_IDS`).
- 비밀번호 분실: 로그인 화면의 **아이디 찾기 / 비밀번호 재설정**(이름 + 가입 이메일로 확인)
  또는 `account_store.reset_password(uid, new_pw)`.
- ⚠️ 시트의 `비번` 칸은 **해시**라 사람이 읽거나 되돌릴 수 없음(재설정만 가능).
- `APP_PASSWORD`는 옛 공용 비밀번호 흔적 — 지금 로그인 경로에서 안 씀.

### 지난주 데이터를 수동 임포트
`app/_import_0415.py`를 참고해서 `MAIN_BODY_MAPPING`과 `WEEK` 변수만 바꿔 실행. 1회용 스크립트.

### 매주 운영 (누구나 — 담당자 구분 없음)
1. 팀원들이 목~화 사이 웹에서 작성 (수요일 기준이라 수요일 회의 전까지)
2. **📝 업무보고 작성·취합** 메뉴 → **📊 제출현황·취합본 생성** 탭에서 제출 현황 확인 → 미제출자 독촉
3. 수요일 회의 전: 같은 탭 하단 "📥 HWPX 생성 및 다운로드" → 파일 받아 회의에서 띄움
   (구 "담당자 대시보드"는 없어지고 제출현황·취합·미리보기가 이 탭으로, 공지·백업은 홈 하단 토글로 분산됨)

### 담당자 개념 제거 · 기능 분산 (2026-07)
담당자(관리자)가 따로 없어져서 **단일 비밀번호 + `is_admin`/`admin_page` 완전 제거**. 구 담당자 대시보드 기능 분산:
> ※ 이후 **개인 계정으로 전환**되면서 `is_admin`이 다시 생겼지만, 쓰임새는
> **`👤 회원 관리`(가입 승인) 하나뿐**이다. 아래 "전원 개방" 원칙은 그대로 유효.
- `member_page()`는 이제 **탭 2개**: `_report_write()`(내 보고 작성) + `_report_collect()`(제출현황·미리보기·HWPX 취합본 생성).
  st.tabs는 **두 탭 본문을 매 실행마다 모두 렌더**하므로 두 함수의 위젯 라벨/키가 겹치면 안 됨(현재 안 겹침, 취합 주차는 `key="collect_week"`).
- 공지 등록/관리(`_notice_manage`)·전체 백업(`_backup_section`)은 **홈 하단 토글**(누구나).
- 구매 완료처리·전체삭제도 **전원 개방**(전체삭제는 ⚠️확인 체크 유지).
- 메뉴 `업무보고 작성` → `📝 업무보고 작성·취합`으로 개명(사이드바·dispatch·홈 바로가기 target 3곳 동기화).

## 스마트돌봄스페이스 페이지 (2026-06 추가)

사이드바 "🏠 스마트돌봄스페이스" 메뉴 — 팀원 누구나 사용. 탭 2개:

| 탭 | 대상 시트 (운영자) | 동작 |
|----|-------------------|------|
| 📖 사용매뉴얼 FAQ 수집 | "스마트돌봄스페이스 FAQ" (백정은) | FAQ 항목 등록 + 전체 목록 조회 |
| 🔧 관리대장 (문제 접수) | "스마트돌봄스페이스 관련 문제 및 돌봄로봇 관리" 중 `스페이스 관리대장` 탭 (한벼리) | 문제 접수 + 미해결 목록 조회 + **완료 처리** |

- 코드: `app/space_store.py` (시트 연동) + `streamlit_app.py`의 `space_page()`
- **시트 ID는 코드·이 문서에 없음** — secrets `[smart_space]` 섹션(`faq_sheet_id`, `space_sheet_id`)에만 둠.
  레포가 공개라서 ID가 노출되면 외부인이 시트에 접근할 수 있기 때문 (실제 값은 로컬
  `app/.streamlit/secrets.toml`과 Streamlit Cloud Secrets에 있음)
- **각 시트에 서비스 계정(`streamlit-bot@…`)이 '편집자'로 공유되어 있어야 함.** 안 되어 있으면
  앱 화면에 공유 안내가 뜸 (앱이 죽지는 않음)
- 시트 컬럼 구조가 바뀌면 `space_store.py`의 `FAQ_HEADER` / `SPACE_LOG_HEADER`만 맞춰 수정
- 앱이 하는 일: **행 추가 + 조회 + 관리대장 완료 처리**. 완료 처리는 해당 행의
  진행상황(G)/조치일자(H)/조치자(J), 조치 내용 입력 시 조치방안(E) 칸만 수정하며,
  쓰기 직전에 그 행의 '문제' 텍스트를 재확인해 행 밀림을 감지함(`RowMismatch`).
  그 외 수정·삭제는 구글시트에서 직접

## 구매요청서 페이지 (2026-06 추가)

사이드바 "🛒 구매요청서" 메뉴 — 팀원 누구나 사용. 장비·재료 구매요청용.

- 코드: `app/purchase_store.py` + `streamlit_app.py`의 `purchase_page()`
- **별도 시트를 안 만듦** — 업무보고 제출함과 **같은 스프레드시트**(secrets `[sheet] id`)에
  `구매요청` 탭(워크시트)을 자동 생성해 사용. 서비스 계정이 이미 편집자라 추가 공유·secrets 불필요.
- 입력: 요청자 + 구매사유 + **품목 표**(`st.data_editor`, 동적 행). 단가×수량=합계, 총액 자동 계산.
- 저장: **한 번의 요청 = 품목 여러 행**, 같은 `요청ID`(`YYYYMMDD-HHMMSS-이름`)로 묶음.
  `구글시트에 저장`은 행 append, `엑셀 양식 다운로드`는 첨부 구매요청서와 같은 xlsx 생성(openpyxl).
- 컬럼 구조가 바뀌면 `purchase_store.py`의 `PURCHASE_HEADER`(시트) / `XLSX_HEADER`(엑셀) 수정.
- 누적 목록은 요청ID별로 묶어 **처리 대기 / 구매완료**로 나눠 표시.
- **구매완료 처리**(진행상황·처리일자·처리자 기록)는 **누구나** 가능(2026-07 담당자 구분 제거) —
  요청 단위(같은 요청ID 전체 행)로 `resolve_purchase()`가 K/L/M 칸만 수정.
- 상태 컬럼(진행상황/처리일자/처리자)은 `PURCHASE_HEADER` 뒤에 추가됨. `_ws()`가
  기존 탭의 헤더/열수를 자동 보정하므로 상태 컬럼 없이 들어간 옛 행도 '대기'로 처리됨.
- **누적 리스트 관리**(2026-06 보완): 📥 누적 전체 **엑셀 다운로드**(`build_purchase_list_xlsx`,
  요청자 열을 비고 옆에 배치), 🗑️ **선택 삭제**(요청ID 단위) / **전체 삭제**(누구나,
  ⚠️확인 체크 필수). 함수: `delete_purchase_request`, `clear_all_purchases`.
- 모든 요청자는 하나의 `구매요청` 탭에 누적(요청자 무관) — 별도 분리 없음.
- 그 외 수정은 시트에서 직접.
- `requirements.txt`에 `openpyxl` 추가됨(엑셀 생성용).

## 문서 협업 보드 페이지 (2026-06 추가)

사이드바 "📋 문서 협업" 메뉴 — 팀원 누구나. 엑셀·워드·PPT를 여럿이 나눠 작성할 때 쓰는 보드.

- 코드: `app/collab_store.py` + `streamlit_app.py`의 `collab_page()`
- **앱은 파일을 저장하지 않음.** 실제 문서는 요청자가 만든 **구글 시트/문서/슬라이드**에 있고,
  앱은 그 **링크 + 요청사항 + 제출현황(완료자)** 만 제출함 스프레드시트의 `문서협업` 탭에 텍스트로 관리.
- **왜 링크 방식인가 (중요·인계 포인트)**: 서비스 계정은 **구글 드라이브에 파일을 저장 못 함**
  (`Service Accounts do not have storage quota` — 공유 드라이브=유료 Workspace 필요). 그래서
  "앱이 업로드 파일을 보관"하는 길이 막혀, 요청자가 직접 만든 구글 문서 **링크만** 받는 구조.
  앱이 파일을 *만들어 즉시 다운로드*(HWPX 취합본 등)하는 건 저장이 0이라 됨 — '만들기'와 '보관'은 다름.
- 흐름: 요청자가 구글 문서 만들어 링크 등록(+요청·마감·담당) → 팀원이 보드에서 링크 클릭해 실시간
  편집 → "✅ 내 부분 완료" 체크 → 요청자/누구나 "🏁 마감". 진행중/완료로 목록 분리.
- 한글(HWP)은 구글이 못 열어 제외. **PPT도 '파일 올리기'에서 제외** — 구글 슬라이드 변환 시
  서식이 깨져서. PPT는 **OneDrive/파워포인트 온라인 편집 링크**를 '링크 붙여넣기'로 등록(안 깨짐).
  즉 자동 구글변환 업로드는 **엑셀·워드만**, PPT·기타는 링크 방식.
- 컬럼이 바뀌면 `collab_store.py`의 `COLLAB_HEADER`만 수정. `_ws()`가 헤더 자동 보정.

### OAuth 파일 자동업로드 (2026-06 추가)

요청자가 **파일만 올리면 앱이 구글 문서로 자동 변환·공유·링크생성**까지 함. (링크 직접 붙여넣기도 여전히 가능)

- secrets `[google_oauth]`(client_id/client_secret/refresh_token)가 있으면 활성 — `collab_store.drive_enabled()`.
  없으면 자동으로 '링크 직접 붙여넣기'로 폴백(앱 안 죽음).
- **왜 OAuth인가**: 서비스 계정은 드라이브 저장 불가(quota 0 — 용량플랜과 무관한 구글 규칙).
  그래서 **본인(carerobot0001) 구글계정**을 OAuth로 연결해 그 드라이브에 파일을 만든다(구글원 5TB
  사용 중이라 용량 여유 충분). 범위는 `drive.file`(앱이 만든 파일만 접근, 비민감 범위).
- **토큰 발급(1회)**: 레포 루트 `get_oauth_token.py` 실행 → 브라우저 인증 → `oauth_secrets_block.txt` 생성
  → 그 내용을 로컬 `secrets.toml` + Streamlit Cloud Secrets에 `[google_oauth]`로 붙여넣기.
  (Google Cloud Console에서 OAuth 동의화면 구성 + 데스크톱 OAuth 클라이언트 생성이 선행. 프로젝트는
  `molten-guide-469800-e0`. `drive.file`은 비민감이라 '앱 게시'해도 구글 심사 불필요 → 토큰 무기한)
- `create_drive_doc(file_bytes, filename)`: 확장자로 변환 대상 결정(xlsx→시트, docx→문서, pptx→슬라이드),
  '링크가 있는 사용자 편집' 공유 후 편집링크 반환. 만든 문서는 본인 드라이브에 **영구 보관**(앱이 안 지움).
- 비밀파일: `client_secret*.json`, `oauth_secrets_block.txt`, `get_oauth_token.py`는 `.gitignore` 처리됨.

## 장비 사용현황 페이지 (2026-06 추가)

사이드바 "🔧 장비 사용현황" 메뉴 — 팀원 누구나. 실증 장비(기기) 사용 대장.

- 코드: `app/equip_store.py` + `streamlit_app.py`의 `equip_page()`
- 출처: `장비 사용 현황_자동화.xlsx`의 마스터 시트("기기 사용현황"). 엑셀의 **연구별 시트
  자동분류(수식)** 는 앱에서 **연구 필터 드롭다운**으로 대체 — 마스터 1개만 관리.
- 데이터: 제출함 스프레드시트의 `장비현황` 탭. `EQUIP_HEADER`(기기명/S/N/자산번호/연구/
  플랫폼/관련앱계정/피험자명/기간/비고). 초기 32행은 엑셀에서 임포트해둠.
- **조회**: 연구(실증)별 필터 + 현재 보기 엑셀 다운로드(`build_equip_xlsx`).
- **편집**: "전체 목록 편집"(토글) → `st.data_editor`(행 추가/수정/삭제) → `save_all_equipment`
  가 데이터영역을 **통째로 덮어쓰기**(동시편집 시 마지막 저장 우선 — 소규모 운영 적합). 누구나 가능.
- ⚠️ 피험자명·계정 등 **개인정보 포함** — 팀 로그인 뒤에서만 노출(외부 공개 없음).
- 컬럼이 바뀌면 `EQUIP_HEADER`만 수정. `_ws()`가 헤더 자동 보정.

## 홈 대시보드 / 실증 방문 일지 (2026-07 추가)

- **🏠 홈** (사이드바 첫 메뉴, 로그인 후 기본 화면): `home_page()`. 구성(위→아래):
  - **상단 전체폭 📅사업단 일정 임베드 달력**(기본 월간 `embed_url("MONTH")`; 주/월/일정 전환은 임베드
    자체 버튼 사용, 앱 라디오는 제거). 제목 옆에 **"나는 누구?" 이름 선택기**(`key="me_widget"` →
    `session_state["me"]`, `?me=`로 새로고침 복원. **`me`는 `main()`에서 시드**되어 전 페이지 사용) —
    접속 후 한 번만 고르면 유지되고 '내 할 일' + **모든 이름 selectbox 기본값**(`_me_index()` 헬퍼로 공지
    작성자·업무보고 본인·구매 요청자·문서협업 내이름·방문일지 방문자·FAQ 작성자)에 자동 적용(변경 가능).
  - 그 아래 **2단** `st.columns([1.7, 1])`:
    - **좌(넓음)**: 📌공지사항(**표시만**) → ⚡**바로가기 타일**(네이버 아이콘식 8개, 4개×2줄,
      **아이콘+라벨을 한 버튼에** `f"{emoji}  \\n{label}"`로 넣어 겹침 방지; `.qbar-mark` 컨테이너에
      스코프된 CSS로 타일화, `::first-line`으로 아이콘만 크게) → 🔔오늘 챙길 것(주간보고 **화 17시 마감
      카운트다운**·문서협업 마감) → 🙋내 할 일(상단에서 고른 `me` 사용→미제출·협업 담당).
    - **우**: 📰**뉴스 섹션 탭**(오른쪽엔 뉴스만).
  - **홈 하단 전체폭 토글(누구나)**: ➕일정 추가·수정·삭제(`_calendar_manage`) · 📌공지 등록/관리
    (`_notice_manage`) · 🗄️전체 데이터 백업(`_backup_section`). 셋 다 기본 접힘(세션키 home_*_open).
  - 전역 여백: `main()`에서 `.block-container` 패딩 축소(layout=wide 기본 상단·좌우 패딩이 큼).
  - 좌 컬럼 안의 바로가기·삭제행은 **컬럼 중첩 1단**(Streamlit 허용 한계) — 더 깊게 넣지 말 것.
    지표/일정 읽기는 try/except로 감싸 하나 실패해도 뜸. 홈 렌더 시에만 주입되는 **컴팩트 CSS**로 압축.
    버튼 네비게이션은 사이드바 radio `key="main_menu"` + `_nav_to`(_goto)로 구현.
  - **공지사항**(`notice_store`, `NOTICE_HEADER=[등록일시,작성자,내용,만료일]`): 누구나 등록/삭제(홈 하단 토글).
    등록 시 **작성자 선택**(`team_config.NOTICE_AUTHORS`=송원경 과장·임명준 연구관·이정아 연구사
    +연구원10명+직접입력). **만료일(표시 종료일)** 지정 시 그날 지나면 화면에서 숨김 +
    `sweep_expired`(세션당 1회, 만료일 기준·높은행부터·등록일시 재확인)로 시트 자동정리.
    미지정이면 영구. **`add_notice`는 `value_input_option="RAW"`** — USER_ENTERED면 "YYYY-MM-DD"를
    시트가 네이티브 날짜로 바꿔 로케일 표기로 되돌려 문자열 비교(`is_expired`)가 깨짐(리뷰 확인 버그).
  - **문서협업 자동 공지**: 진행중 문서협업(`collab_rows`, 상태≠완료)을 공지 영역에 **가상 렌더**
    (시트 저장·삭제 없음) — 제목·마감·`N/M명 제출·남은 사람`(완료자=제출 체크) 표시, 협업 완료·삭제
    시 자동 소멸. `add_collab`을 건드리지 않아 안전.
  - **뉴스 섹션 탭**(`news_store.NEWS_SECTIONS`): 돌봄로봇/돌봄·복지/AI·신기술/로봇·휴머노이드 4탭,
    탭마다 다른 키워드로 `fetch_section(specs)` 조회(각 ttl 1h). 구글뉴스 RSS라 사진 없이 제목·출처.
    - ⚠️ **국내만 나오던 원인**(2026-08 수정): 구글뉴스는 `hl/gl/ceid`로 언어·지역이 정해지는데
      전부 `hl=ko&gl=KR` 이었다. 키워드를 아무리 영어로 넣어도 한국 기사만 나온다.
      지금은 섹션마다 `(키워드, 로케일)` 쌍으로 KO/EN/JA를 함께 조회하고 `region`을 붙인다.
    - 담을 때 **라운드로빈**(키워드마다 한 건씩 번갈아) — 한 키워드에서 몰아 담으면
      뒤쪽 해외 키워드가 cap에 걸려 아예 안 나온다.
    - ⚠️ RSS의 `description`은 **기사 요약이 아니라 링크 HTML**이다(확인함). 요약을 붙이려면
      기사 본문을 읽는 별도 수단(모델 호출 등)이 필요하다 — 지금은 제목·출처만.
    - **앞쪽 탭 = 복지부 11개 과제**(2026-08 변경). 이동·목욕·배설·유연착용형·인체영향성·
      모니터링·이승·욕창·식사·커뮤니케이션·AI 챗봇. 예전엔 주제별 5탭(돌봄로봇·돌봄정책·
      AI·LLM·AI 활용법·로봇휴머노이드)이었는데 **과제와 대응이 안 맞아** 기사를 사람이
      다시 갈라야 했다. 'AI 활용법' 키워드(프롬프트·RAG·업무자동화)는 11번 AI 챗봇으로 옮김.
      **`돌봄·정책`만 남겼다** — 과제 키워드가 전부 '기기'라 제도·수가·인력 소식은
      어느 탭에도 안 걸린다. `AI·LLM`·`로봇·휴머노이드`는 없앴다.
      ⚠️ 그래서 **일반 AI 모델·휴머노이드 플랫폼 소식은 이제 어디에도 안 뜬다**(확인:
      과제 탭 54건 중 휴머노이드·LLM 언급 1건). 필요해지면 탭을 되살릴 것.
      일반 뉴스는 **경제·시장 하나만**(맨 뒤). 세계·한국·축구는 뺐다.
      11 AI 챗봇 키워드는 `11. AI챗봇/AI챗봇_유사서비스_자료조사_202608.xlsx`(자체 조사)의
      조사 축에서 옮겼다 — AI 안부전화·AI 생활지원사·디지털휴먼·시니어 헬스케어.
    - **개수 제한 없음 · 최근 24시간만**(2026-08 지시). 예전엔 섹션당 6건으로 잘랐다.
      - 하루 지난 기사를 두 겹으로 막는다: ①검색어에 `when:1d` ②`pubDate` 재확인
        (토픽 피드는 `when:` 을 못 붙여 ②가 필요하다). 최신순 정렬.
      - **관련 없으면 안 채운다.** 그날 그 과제 뉴스가 없으면 빈 탭이고 화면엔
        '오늘은 새 뉴스가 없습니다'. (실제로 하루에 0건인 과제가 생긴다)
      - ⚠️ 구글뉴스는 낱말을 **느슨하게** 맞춘다 — `노인 이동 보조 로봇` 에
        'AI 약물안전 투자유치'가 딸려 왔다. 6건만 담을 땐 안 보이던 게 제한을 없애니
        쌓였다 → `_relevant()` 로 **제목에 검색어 낱말이 실제로 든 것만** 남긴다.
        **국문은 1개, 영문은 2개** — 영문은 `mattress` 하나로 침구 쇼핑 기사가
        딸려 왔다("The 11 Best Cooling Mattresses"). `_STOP` 의 흔한 말은 판정에서 뺀다.
      - ⚠️ 해외 기사가 수십 건이 되면서 제목 번역이 **직렬로는 몇십 초**가 걸렸다 →
        `ThreadPoolExecutor(8)`. 워커에는 스크립트 컨텍스트가 없어 `st.cache_data`
        붙은 `translate` 대신 맨 함수 `_translate` 를 쓴다(결과는 `fetch_section` 캐시에 남음).
    - **키워드 출처**: `과제별_자료조사_키워드_프롬프트_양식.xlsx`(각 과제 담당자가 직접 작성).
      ⚠️ 이 엑셀은 **담당자 이름·프롬프트가 들어 있어 공개 레포에 커밋하지 않는다** — 로컬에만.
      ⚠️ 엑셀 키워드를 **그대로 다 넣지는 못한다**: ①논문 검색용 불리언(9번 식사의
      `(A OR B) AND (C) AND (D)`)은 구글뉴스 RSS가 못 삼킴 → 핵심 명사만 ②학술 용어
      (CycleGAN·Domain Adaptation·근전도)는 뉴스가 안 잡힘. **실제 조회해 수확되는 것만** 남겼다.
      - `외골격 로봇`은 **0건**이다(붙여 쓰면 안 잡힘) → `착용형 로봇`·`엑소슈트`를 쓴다.
      - 2·3번(목욕·배설)은 엑셀이 비어 있어 **탭만 만들고 비워 뒀다**(안내 문구 표시).
        키워드가 들어오면 `NEWS_SECTIONS`의 빈 튜플만 채우면 된다.
    - ⚠️ 키워드 검색은 **판단**("투자 헤드라인 말고 실질", "왜 중요한가")을 못 한다.
      맞출 수 있는 건 *주제 범위*까지다.
    - **일반 뉴스 4탭**(세계·한국·경제·축구)은 매일 받는 뉴스 브리핑 PPT의 섹션 구성을
      가져온 것. 이런 넓은 분야는 키워드 검색보다 **구글뉴스 토픽 피드**가 깨끗하다
      (편집된 헤드라인이 온다) → 스펙에 `topic:WORLD` 처럼 쓰면
      `/rss/headlines/section/topic/…` 으로 조회한다.
    - `fetch_section`은 **소스 수에 맞춰 per_query를 올린다** — 키워드가 하나뿐인
      섹션(한국=NATION 토픽 하나)은 고정 per_query면 항목이 3개밖에 안 찼다.
    - **오역 교정 사전**(`_GLOSSARY`): 기계번역이 기술 용어를 통째로 틀리는 것만 바로잡는다.
      `prompt engineering` → '신속한 엔지니어링'(prompt를 '신속한'으로 옮김)이 대표적.
      ⚠️ 뜻을 바꾸는 의역은 넣지 말 것 — **틀린 게 확인된 것만**.
    - **하루 1회 수집 · 매일 교체**: `fetch_section(specs, day)` 의 `day`(=`today_key()`,
      KST 날짜)는 **안 쓰는 인자처럼 보이지만 캐시 키**다. 날짜가 바뀔 때만 다시 수집하고
      같은 날에는 처음 연 사람이 가져온 목록을 하루 종일 함께 본다. 예약 실행 장치가
      없어도 되는 방법(ttl은 이틀 — 날짜 키가 먼저 갈린다).
      ⚠️ 프로세스가 재시작되면 캐시가 비어 그날 다시 한 번 수집한다(같은 날이라도).
    - **해외 제목 번역**(`news_store.translate`): 구글 번역의 **키 없는 endpoint**
      (`translate.googleapis.com/translate_a/single?client=gtx`). 무료·무설정이지만
      **공식 API가 아니라 막히거나 바뀔 수 있다** → 실패를 정상 경로로 보고 원문으로 되돌린다.
      `@st.cache_data(ttl=86400)`으로 제목당 하루 1회만 호출.
    - ⚠️ **기계번역은 틀린다.** `令和8年度`(2026년도) → '2007년'으로 나온다(MyMemory도
      '2018회계연도'로 틀림). 그래서 화면에 **원문 제목을 회색으로 함께** 둔다.
      숫자·연도를 보고에 옮길 때는 원기사를 봐야 한다. [[no-fabrication]]
- **📍 실증 방문 일지**: `app/visit_store.py` + `visit_page()`. 제출함 스프레드시트의
  `방문일지` 탭. `VISIT_HEADER`(방문일/실증/방문자/방문내용/이슈/등록일시). 등록 + 실증별
  필터 조회 + 삭제(등록일시 재확인으로 행 밀림 방지). 누구나.
- 사이드바 메뉴가 10개가 됨(🏠홈·🖥️주간취합·📝업무보고 작성·취합·🏠스페이스·🛒구매요청서·
  📋문서 협업·📁자료실·🔧장비 사용현황·📍실증 방문 일지·📚과거 회의록 열람, +관리자만 👤회원 관리).
  카테고리로 묶어 표시(`_cats`). `streamlit_app.py`가 3000줄을 넘음 → **페이지 파일 분리 리팩토링**
  하기 좋은 시점(기능 동작엔 문제없음, 유지보수 편의 목적). [[weekly-report-app-roadmap]]

## 사업단 일정 (구글 캘린더 연동, 2026-07 추가)

**별도 사이드바 메뉴가 아니라 🏠 홈 안에 임베드됨**(우측 컬럼 달력 + 하단 전체폭 관리 토글).
`calendar_page()`는 없고, 홈이 `embed_url()`로 임베드 + `_calendar_manage()`로 추가/수정/삭제한다.

- 코드: `app/calendar_store.py` + `streamlit_app.py`의 `home_page()` / `_calendar_manage()`
- **서비스 계정으로 양방향 연동** — 드라이브 파일과 달리 캘린더 이벤트는 서비스 계정이
  저장/수정 가능(용량 이슈 없음). Calendar API 사용설정 + 대상 캘린더를 서비스 계정에
  **'일정 변경' 권한으로 공유**해야 함.
- 캘린더 ID는 secrets `[calendar] id`(공개 레포 대비). 현재 캘린더 = **돌봄로봇중개연구사업단**
  (`pi7uilph8s...@group.calendar.google.com`). 없으면 홈에 설정 안내가 뜸.
- 임베드: `embed_url(mode)` — `mode`는 `WEEK`(홈 기본)/`MONTH`/`AGENDA`(일정목록). 홈 우측에서
  라디오로 전환. `st.iframe`(신)/`components.iframe`(구) 버전 호환 처리. 임베드는 구글 로그인·권한
  있는 사람에게만 보이므로, 필요 시 `AGENDA`(일정목록)로 텍스트 목록처럼 볼 수 있음.
- CRUD: `upcoming_events`(ttl 60s)/`add_event`/`update_event`/`delete_event`. 종일/시간 지정 지원.

## 사업단 공통확인사항(최혜민) 페이지 (2026-07 추가, 1단계)

**별도 사이드바 메뉴가 아니라 「📝 업무보고 작성·취합」의 3번째 탭 `📑 사업단 공통확인사항`**(2026-07 이동).
`member_page()`의 st.tabs가 작성/취합/공통확인 **3탭을 매 실행마다 모두 렌더**하므로 `common_page()`의 위젯이
`_report_write`/`_report_collect`와 키·라벨이 겹치면 안 됨(현재 안 겹침 — data_editor는 `ce_*` 키). 홈 바로가기
"공통확인" 타일은 이 페이지로 이동(첫 탭에 착지하니 사용자가 공통확인 탭 클릭).

최혜민 연구원의 표(본부과제 용역·자산구매 × 실적/계획)를 앱에서 입력 → **한글(HWPX)/엑셀 생성**. 표라서 별도 처리.

- 코드: `app/common_store.py` + `streamlit_app.py`의 `common_page()`(업무보고 탭에서 호출)
- 저장: 제출함 스프레드시트 `공통확인사항` 탭(종류/구분/내용1~4). `st.data_editor` 4개.
- **한글 생성 방식(중요)**: 취합본의 **리프 표**(중첩 없는 `<hp:tbl>`)를 찾아 — 용역 표(발주금액,
  최대 5행)·자산 표(구매금액, 최대 10행), 각 실적(1번째)/계획(2번째) — 검증된 `replace_cell`로
  셀을 채움(합계 자동). 동적 행 삽입은 위험해서 **고정 행수**만. 행 초과 시 UI 경고.
- **템플릿**: `사업단_공통확인사항_템플릿.hwpx`(레포 루트). 06.24 취합본을 **빈 표로 변환**
  (모든 데이터·피험자명 제거, 헤더만 남김) 후 커밋 — 데이터 든 원본은 **커밋 금지**(공개 레포).
  표 구조가 바뀌면 이 템플릿을 새로 만들어야 함(위 blanking 스크립트 참고).
- 엑셀도 제공(`build_common_xlsx`) — 한글이 안 맞으면 대체용.
- ⚠️ HWPX는 예민해 로컬에서 '한글 열림'을 검증 못 함 → 최혜민 님이 실제 열어 확인 필요.
- **2단계(보류)**: 이 표를 메인 취합본 한 파일에 합치기 = 다른 10명 셀 인덱스가 밀려 재매핑
  필요 + 깨짐 위험 큼. 1단계(별도 한글) 안정화 후 검토.

## 팀 할 일 '요청' 배정 (2026-07 추가)

한 사람이 다른 팀원에게 일을 부탁하고, 완료·회신을 앱 안에서 주고받는 기능.
카톡으로 흩어지던 "○○ 해주세요"를 한 곳에서 관리하려고 만듦. **메일 발송 불필요.**

- 코드: `app/request_store.py` + `home_page()` 안 (받은 요청=왼쪽 '내 할 일',
  보내기 폼=오른쪽 '정보 미입력 일정' 아래 — 좌우 길이 균형 때문)
- 데이터: 제출함 스프레드시트 `팀요청` 탭.
  `REQ_HEADER=[요청ID,요청자,대상,내용,등록일시,상태,완료일시,링크,회신,확인]`
  (뒤 3개는 나중에 추가 — **맨 뒤에 붙여야** 옛 행이 안 밀림)
- **흐름**: 보내기 → 받는 사람 홈에 `📨 받은 요청` → **✓ 완료** 또는 **💬 한 줄 회신**
  → 요청자 홈 `🔔 오늘 챙길 것`에 결과 알림 → **[확인]** 누르면 사라짐(`확인` 칸 기록)
- **여러 명에게 보내기**: `add_requests()`가 **대상마다 한 행** 생성(각자 따로 완료·회신).
  ⚠️ 요청ID에 **대상 이름을 포함**해야 함 — 같은 초에 만들면 ID가 겹쳐 남의 행이 처리됨.
  '내가 보낸 요청'은 (내용+등록일시)로 묶어 `3/9명 완료`처럼 한 줄로 표시.
- **대상 명단**: 고르기=`USER_NAMES`(연구원 10 + 간부 3), '연구원 전체에게 보내기'
  체크=`MEMBER_NAMES`만, 그 외는 **직접 입력**(쉼표 구분). 셋을 합쳐 중복·본인 제거.
- **✎ 수정 / ✕ 회수**: 잘못 보냈을 때. 수정은 내용·링크만 바꾸고 완료·회신 기록은 보존.
  회수는 받은 사람 전원에게서 삭제. ⚠️ 이미 본 사람에겐 소용없음(따로 알려야 함).
- 문서 작업이 **여러 명 분담**이면 이 기능 말고 **📋 문서 협업**(마감·제출현황 추적)이 맞음.

## 내 할 일 — 업무/개인 구분과 보고 연계 (2026-07)

- `todo_store` 구분: `할일`(업무) / `챙길것` / `완료`(기록) / `_sync`(내부).
  (`개인`은 아래처럼 시트를 안 씀 — 옛 데이터 읽기·이관용으로만 남아 있음)

### 🔒 개인 할 일은 브라우저에만 저장 (2026-08)
제출함 스프레드시트는 **팀 공용**이라, 시트를 열 수 있는 사람에게 개인 메모가
그대로 보였다. 그래서 개인 할 일은 **서버·시트로 보내지 않고** 브라우저
localStorage(`ds_per_<uid>`)에만 둔다.
- `personal_todos(uid)` / `save_personal(uid, items)` (`streamlit_app.py`).
  항목은 `{"t": 내용, "ts": 등록시각}`. 읽기는 세션당 1회, **쓰기는 `main()` 끝에서
  1회** — 자동로그인 저장과 같은 방식(리런 중 컴포넌트 쓰기가 잘리는 것 방지).
- 추가·완료·업무↔개인 이동 모두 시트를 거치지 않는다.
  업무→개인은 *시트에서 삭제 + 브라우저에 추가*, 개인→업무는 그 반대.
- 옛 데이터: 시트에 남은 `개인` 행이 있으면 홈에 경고와 **'내 기기로 옮기고
  시트에서 지우기'** 버튼을 띄운다(삭제는 되돌릴 수 없어 자동으로 하지 않음).
- ⚠️ **기기별 저장** — 폰과 PC가 공유되지 않고, 브라우저 데이터를 지우면 사라진다.
  잃으면 곤란한 것은 '업무'로 두라고 화면에도 안내한다.
- **개인 할 일은 주간보고에 절대 안 들어감**(애초에 시트에 없음) — 보고 작성이 읽는 건 `list_todos()`(업무만)와
  `completed_todos()`(완료 기록)뿐이고, 완료 기록은 `complete_todo()`에서만 생기는데
  그건 업무 항목에만 연결돼 있음. 개인은 `delete_todo()`로 그냥 삭제.
- **완료한 업무 → 업무실적**: 홈에서 ✓ 누르면 삭제 대신 `완료` 기록으로 남고, 업무보고
  작성 화면의 `✅ 완료한 할 일을 업무실적에 넣기`에서 그 주기 완료분을 골라 넣음.
- **업무 ↔ 개인 이동**: 머리글의 `🔀`를 눌러야 각 항목에 이동 버튼이 보임(평소엔 ✓만).
  토글은 `?go=move_on/off` 링크로 처리(`_inline_plus(extra=...)`).

### ⋯ 진행 메모 → 주간보고 실적 (2026-08)

**✓(완료)를 누르면 실적 기록이 되어버리는데 아직 끝난 게 아니고, 그냥 두면 손도 안 댄
일처럼 보인다.** 그 사이를 메운다. 처음엔 '회신대기' 고정 상태로 만들었다가, 사정이
항목마다 다르므로(회신 대기·초안 작성 중·검토 요청함…) **본인이 직접 쓰는 한 줄**로 바꿨다.

- 시트: `TODO_HEADER` 맨 뒤에 `진행일`·`진행` 추가(옛 행 안 밀림).
  `진행`=자유 텍스트, `진행일`=적은 날짜(며칠째인지 세기 위해).
  `todo_store.set_note(uid,row,text,note)` — 두 칸이라 `_update`를 두 번 부른다.
  ⚠️ 컬럼을 늘렸으면 `append_row([...])` **3곳**(`set_sync`·`add_todo`·`complete_todo`)의
  칸 수도 같이 늘려야 한다. 안 그러면 마지막 칸이 비는 게 아니라 헤더와 어긋난다.
- ⚠️ **컬럼을 늘려도 시트가 안 늘어나는 함정**(여기서 실제로 저장이 조용히 실패했다):
  `_ws()`는 `@st.cache_resource`라 **프로세스가 사는 동안 한 번만** 헤더를 확인한다.
  Streamlit Cloud가 재배포에서 프로세스를 안 죽이면 새 칸이 영영 안 생긴다 →
  `todo_store._ensure_cols(ws)`를 쓰기 직전(`_update`·`add_todo`)에 부른다.
- 화면: 항목 옆 `⋯` → 그 줄 아래 입력칸(저장/지우기). 메모가 있으면 항목 아래
  `↳ 메모 · N일째`(`_todo_note_line`). 정렬은 건드리지 않는다 — 진행 중이라고 아래로
  내리면 오히려 잊는다.
- **주간보고 연계**: 업무보고 작성의 `✅ 완료·진행 중인 할 일을 실적에 넣기`에
  완료분과 함께 뜨고, `(진행) 내용 — 메모` 형태로 실적 칸에 들어간다.
- **☆(중요)는 없앴다**(2026-08-07) — 드래그로 순서를 정할 수 있게 되면서 '맨 위로'와
  겹쳤다. 시트의 `중요` 칸과 `set_star`는 옛 데이터 때문에 남겨 뒀고, 정렬에서도 뺐다.
- 기호는 **글자**(`⋯`)를 쓴다 — 이모지는 CSS 색이 안 먹어 다크모드에서 흰 박스가 된다.
  버튼 키 `todo_wait_`(이름은 옛 흔적)를 작은버튼 높이 CSS와 다크모드 색 규칙
  **양쪽에** 넣어야 한다.

### 묶음(프로젝트) · 완료 후 이어가기 (2026-08)

**큰 일 하나가 작은 일로 계속 갈라지는데 ✓를 누르면 그 갈래가 사라진다.**
무엇의 일부였는지 안 남아서, 주간보고에 조각만 늘어놓게 된다.

- `TODO_HEADER` 맨 뒤에 **`묶음`** 추가(옛 행 안 밀림). `set_group()` / `groups(uid)`.
  ⚠️ 컬럼을 늘렸으니 `append_row` **3곳**(`set_sync`·`add_todo`·`complete_todo`)의
  칸 수도 11개로 맞췄다. `complete_todo`는 묶음을 **완료 기록에 보존**한다.
- 화면: 항목 옆 주황 태그. ⋯ 패널에서 기존 묶음 고르기 또는 새 이름 입력.
- **완료 직후 `after_done`** 세션값이 생겨 업무 상자에 *"이어지는 다음 할 일"*
  입력이 뜬다. 적으면 같은 묶음으로 `add_todo`.
- 주간보고 실적에는 `[묶음] 내용` 으로 들어간다.

### 개인 할 일 정렬 — ▲▼ 버튼 (2026-08)

`streamlit_sortables` 가 **목록을 통째로 안 그리는 경우**가 있었다(캡션과 저장
버튼만 뜸). 원인을 못 잡아, ▲▼ 버튼을 **기본**으로 두고 드래그는 접은 채로 함께
둔다. 버튼은 누르는 즉시 `save_personal` 로 저장한다.

### 메일 → 할 일: 제목 자동 + 본문에서 골라 담기 (2026-08)

`_auto_import()`가 수집 메일을 할 일로 넣지만 **제목만** 넣는다. 정작 "무엇을 하라"는
지시는 본문에 있어서 제목만 보면 일을 알 수 없다 → 업무 머리글에 **📧 버튼**(`mail_pick_btn`)
을 붙여 본문을 읽고 **문장 단위로** 담게 했다(📄 주간보고 picker와 같은 패턴).

- `_mail_ask_lines(body)` — 서명·인용을 골라 지우지 않고, 반대로 **부탁하는 말투인 줄만
  남긴다**(`주세요|주시기|바랍니다|부탁|해야|필요합니다|해 주|송부|회신|제출|중요합니다`).
  서명·주소·전화번호는 자연히 걸러진다. `*`(원본 헤더)·`>`(이전 메일 인용)·`@`·`http` 줄 제외,
  중복 제거, 최대 8줄. 실제 수집 메일 3건으로 검증함.
- `_mail_panel(uid, existing_rows, today)` — 최근 8건, 이미 담은 문장은 회색 '이미 있음',
  `본문 전문 보기` 체크 시 `st.text_area(disabled=True)`.
  ⚠️ 이 패널은 `st.expander`(🏢 업무) **안에서** 호출된다 — expander를 또 쓰면 앱이 죽는다.
- 아이콘 위치: ＋111 · 📄135 · ⇅157 · **📧179** (전부 `top:9px`, 22px 정사각).
  새 아이콘을 넣을 때는 CSS **4곳**(위치·button·button p·hover)과 다크모드 블록까지 함께 고쳐야
  한다 — 한 곳만 빠지면 흰 네모로 보인다.
- **수집 자체가 도는지 확인하는 법**: `메일수신` 탭의 행 수와 `개인할일`의
  `_sync` 행(`mail=<마지막 처리 날짜>`)을 본다. Apps Script 트리거는
  script.google.com → 해당 프로젝트 → ⏰ 트리거 / 실행 로그.
- **매칭 규칙**: `mails_for()`는 **보낸사람 = 내 등록 이메일**인 메일만 내 것으로 본다.
  즉 외부에서 받은 지시는 **대표 계정으로 전달(FW)** 해야 들어온다. 현재 운영 방식이 이것.

## 🔧 기기 자료 — 제조사가 직접 올리는 매뉴얼 (2026-08)

논문 AS-IS **문제 4(공급자와 끊김)** 를 메우는 기능. 현장에서 기기를 만지는 사람이
매뉴얼·문의처를 못 찾는 문제. 대상 제조사는 **500곳 규모로 늘어날 예정**.

- 코드: `app/maker_store.py` + `streamlit_app.py`의 `maker_submit_page()` /
  `_maker_tab()` / `_maker_review()`
- 데이터: 제출함 스프레드시트 `기기자료` 탭.
  `MK_HEADER=[등록일시,제조사,기기명,모델,자료종류,제목,링크,설명,담당자,연락처,상태,검토자,검토일시]`
- **링크 우선, 파일도 받는다.** 링크면 제조사가 갱신하는 순간 최신이라 버전 관리·
  저작권·삭제 요청이 우리 일이 안 된다. 그런데 **중소 제조사는 홈페이지에 자료를
  안 올려두는 곳이 많아 링크만 받으면 제출 자체를 못 한다** → `upload_file()`로 파일도 받는다.
  - `collab_store.create_drive_doc`과 달리 **구글 문서로 변환하지 않는다**(매뉴얼은
    PDF라 변환하면 깨진다). 권한도 `writer`가 아니라 **`reader`**.
  - 같은 OAuth 자격(carerobot0001 드라이브, 구글원 5TB) 재사용. 서비스 계정은 여전히 불가.
  - 형식 `UPLOAD_EXTS` 10종 · 용량 `MAX_MB=20`. 파일 제출은 설명에 `[파일 보관본]` 자동 표기.
  - ⚠️ 파일은 **우리가 보관하는 옛날 판**이 될 수 있다 → 화면에서 개정일·판번호를 적게 안내.
- ⚠️ **`st.form` 안의 위젯은 바꿔도 리런이 안 난다** — 전달 방법(링크/파일) 라디오를
  폼 안에 뒀더니 '파일 올리기'를 골라도 파일 칸이 안 나왔다. **라디오는 폼 밖**에 둔다.
- **흐름**: 제조사 제출(무인, 24시간) → `대기` → 팀원이 확인 → `공개`.
  500곳에 토큰을 하나씩 발급할 수 없어 **셀프 제출 + 검토 후 공개**로 갔다.
- **입구는 같은 앱의 다른 문**: `?maker=<토큰>` 이면 `main()`이 **`auth_gate()` 앞에서**
  `maker_submit_page()`를 띄우고 끝낸다. 토큰은 secrets `[maker] token`,
  없으면 값이 있기만 하면 열림(1단계 시험용).
  ⚠️ `st.set_page_config`는 모듈 맨 위에서 이미 부르므로 **여기서 또 부르면 죽는다**
  → CSS로 사이드바만 숨겨 '폼 하나짜리 페이지'로 보이게 했다.
- **별도 메뉴·별도 사이트를 안 만든 이유**: 팀원이 이미 가는 곳에 있어야 쓴다.
  → 📁 자료실의 **탭**(`📂 팀 자료` / `🔧 기기 자료`)으로 넣고,
  🔧 장비 사용현황의 표 **아래**에 그 목록 기기의 자료를 접어서 붙였다
  (표는 캔버스라 행 안에 링크를 못 넣는다).
- `for_device()`는 **양방향 부분일치** — 대장에는 `WIM B2B (위로보틱스)`처럼 설치 위치·
  괄호가 붙은 이름이 많아 완전일치로는 거의 안 걸린다. 검증: `효돌`·`WIM B2B (위로보틱스)`·
  `EMFIT QS` 매칭됨, `광주서구 1 거실 P2`는 안 걸림(자료 미등록이라 정상).
- 검증한 것: 탭 자동 생성·헤더·제출→`대기` 저장·행 삭제, **드라이브 업로드**(PDF가
  변환 없이 `application/pdf`로 올라가고 reader 공유·삭제까지)를 로컬에서 실제로 확인.
- ⚠️ **외부 공개 전 남은 두 가지**: ①무료 호스팅 절전(`Zzzz` 화면을 제조사가 보면 곤란)
  ②사업단 대외 서비스가 되므로 내부 승인.

## 🎤 음성 입력 (2026-08)

현장에서 손을 못 쓸 때 말로 양식 칸을 채운다. 1단계는 **실증 방문 일지**
(`visit_page()`의 방문내용·이슈).

- 코드: `app/voice_note.py` — `available()` / `apply_buffer(*keys)` / `mic(state_key, label)`
- **브라우저 내장 음성인식**(Web Speech API, `ko-KR`)을 쓴다. 설치·과금 없음.
  `streamlit_js_eval`로 **부모 창**에 인식기(`window.__dsVoice`)를 한 번 만들어 두고,
  🎤(시작) → ■(정지) **두 번의 리런**으로 결과를 받는다. 리런이 나도 창은 그대로라
  인식기가 살아 있다.
- ⚠️ **위젯 키에 직접 못 쓴다** — 이미 만들어진 위젯의 `session_state`는 Streamlit이
  막는다. 그래서 `<key>_buf`에 넣고 **다음 실행에서 위젯 생성 전에** `apply_buffer()`가
  옮긴다. 폼 맨 앞에서 부를 것.
- ⚠️ `streamlit_js_eval`은 **key마다 한 번만** 실행된다 → `_seq()`로 매번 새 key.
- ⚠️ 오류는 `##ERR##<사유>` 표식으로 돌려준다(널 문자를 쓰면 파이썬 소스가 깨진다).
  사유: `unsupported`(사파리 등) · `not-allowed`(마이크 권한) · `empty`.
- ⚠️ **음성이 브라우저 제조사(구글) 서버를 거친다.** 그래서 화면에 *참여자는 이름 대신
  코드로* 라고 안내한다. 완전 로컬이 필요하면 이 기능 말고 녹음 후 PC에서 변환.
- 크롬 계열 전용(안드로이드·PC). iOS 사파리는 안 된다.
- 검증한 것: 모듈 임포트·앱 렌더·인식기 JS가 실제 브라우저에서 `ok` 반환.
  **실제 받아쓰기(마이크 입력)는 로컬에서 로그인할 수 없어 검증 못 했다.**

## 줌 링크 · 취합본 최신성 (2026-07)

- **줌 링크 여러 개**: `zoom_links()` / `save_zoom_links()` — 팀 공용 JSON을
  `todo_store.set_sync("_team","zoom_links")`에 저장. 이름별로 몇 개든(주간회의·세미나…).
  🖥️ 주간취합 페이지의 `⚙️ 링크 설정`에서 편집(이름을 지우면 그 줄 삭제),
  홈 바로가기에 이름대로 타일 생성. 옛 단일 링크(`zoom` 키)는 '주간회의'로 자동 인식.
  ⚠️ 링크·암호는 **코드에 넣지 말 것**(공개 레포) — 시트에만 둔다.
- **취합 후 수정 감지**: 취합본 생성 시각을 `export_{주차}`로 기록해두고, 그보다 늦게
  제출·수정한 사람이 있으면 홈 `오늘 챙길 것`과 제출현황에 경고 → 회의 전 재생성 유도.

## 다크모드 (2026-07)

사이드바 `🌙 다크 모드로 / ☀️ 라이트 모드로` 버튼(토글 스위치는 어두운 사이드바에서
켜짐/꺼짐이 안 보여 버튼으로 대체). 선택은 `todo_store.set_sync(uid,"theme")`에 계정별 저장.

- **색은 CSS 변수 한 곳(`:root`)에만 정의**하고 55곳이 참조 — 톤 조정은 이 5줄만 고치면 됨.
  최종값: `--ds-bg:#141413`(검정) `--ds-surface:#1d1d1c` `--ds-border:#333331`
  `--ds-text:#d1cdc5` `--ds-text2:#97948d` `--ds-accent:#b05a35`, 사이드바 `#0e0e0d`.
- **눈부심 = 배경 밝기가 아니라 '글씨와의 대비'.** 검정 배경에 순백 글씨(14.9:1)면
  "옛날 휴대폰 화면처럼 빛난다". 반대로 너무 낮추면 안 보인다.
  **본문 11~12:1**(현재 11.6), 제목 13.2, 보조 6.1이 사용자가 만족한 지점.
  ⚠️ 여기까지 오는 데 세 번 헤맸다 — ①글씨를 어둡게(안 보임) ②배경을 검정으로만
  (더 쨍해짐) ③대비를 낮춰 해결. **"더 어둡게 = 더 편함"이 아니다.**
- **채도 높은 이모지/주황도 눈부심 원인.** 바로가기 타일은 면을 없애고 테두리만 남기고
  이모지에 `filter:saturate(.55) brightness(.82)`를 걸었다. 주황은 포인트에만.
- 대비는 감으로 맞추지 말고 계산할 것(아래 스니펫). 기준: 본문 4.5:1 이상, 보조 3:1 이상.

```python
# 대비 계산(WCAG) — 색 바꿀 때마다 확인
def lum(h):
    h=h.lstrip('#'); c=[int(h[i:i+2],16)/255 for i in (0,2,4)]
    c=[x/12.92 if x<=.03928 else ((x+.055)/1.055)**2.4 for x in c]
    return .2126*c[0]+.7152*c[1]+.0722*c[2]
def ratio(a,b):
    l=sorted([lum(a),lum(b)],reverse=True); return (l[0]+.05)/(l[1]+.05)
```
- **CSS로 라이트 테마를 덮는 방식이라, 지정 안 한 '상태'가 밝게 남는 게 반복 함정.**
  지금까지 걸린 곳(전부 처리됨): 폼 버튼(`stFormSubmitButton` — `stButton`이 아님),
  확장패널 헤더 `summary`의 hover/open, 상단 바 `stHeader`(사이드바 열기 » 묻힘),
  BaseWeb 팝업(드롭다운·달력 — **body 포털이라 앱 밖에 그려짐**), 백틱/`st.code`,
  바로가기 타일의 크림색 배경, 체크박스.
- **이모지 버튼은 CSS로 색이 안 먹음**(폰트가 그림을 그림) → 흰 박스처럼 보임.
  작은 아이콘 버튼은 글자 기호(`✎`·`✕`·`✓`)를 쓸 것.
- **배경을 어둡게 바꾸면 밝기 기준으로 정해둔 색들이 하나씩 묻힌다.** 특히
  입력칸 안내문구(`::placeholder`)는 브라우저 기본 `opacity`까지 풀어야 보인다.
- 인라인 스타일로 색을 고정한 곳(`_inline_plus` 제목, 홈 타이틀, 주간취합 표)은
  CSS로 못 덮으므로 **파이썬에서 `st.session_state["dark"]`를 보고 색을 분기**해야 함.
- **표(`st.dataframe`/`st.data_editor`)는 캔버스로 그려서** 보통 CSS가 안 먹는다 →
  `--gdg-bg-cell`, `--gdg-text-dark` 같은 **CSS 변수**로 지정.
- 달력·드롭다운 팝업은 내부가 여러 겹이라 하나씩 잡으면 흰 칸이 계속 남는다 →
  **팝업 하위 전체를 덮고** 선택·hover·비활성만 되살리는 방식이 안전.

## ⚠️ 페이지 CSS가 사이드바로 새는 함정 (반복 발생)

`st.markdown("<style>…")`로 주입하는 CSS는 **앱 전체에 적용**된다. 그래서 본문을
촘촘하게 만들려고 넣은 규칙이 **사이드바 메뉴 간격까지 무너뜨린다**(카테고리 소제목이
버튼에 겹쳐 보이는 증상). 실제로 두 번 발생:
- 홈의 컴팩트 CSS(`stVerticalBlock{gap}` 등) → 사이드바 브랜드와 다크모드 버튼이 겹침
- 주간취합의 `gap:0.1rem !important` → 사이드바 메뉴 줄간격 붕괴

**규칙**: 여백·간격·정렬·폭에 관한 CSS는 반드시 **`section[data-testid="stMain"]`**
으로 한정한다(색상만 바꾸는 규칙은 전역이어도 대체로 안전).

```bash
# 새는 규칙 점검 — gap/flex/width 등이 stMain·stSidebar 한정 없이 쓰였는지
cd app && grep -nE "stVerticalBlock|stHorizontalBlock" streamlit_app.py \
  | grep -E "gap|flex-direction|width|padding|margin" | grep -v "stMain\|stSidebar"
```

## 💡 개선 요청 페이지 (2026-08 추가)

앱(Dolbom Studio) **자체**에 대한 팀원 의견을 모으는 곳 — 업무 데이터가 아니라
"쓰다 보니 안 되는 것·있으면 좋겠는 것"을 받는다.

- 코드: `app/feedback_store.py` + `streamlit_app.py`의 `feedback_page()`/`_fb_item()`
- 데이터: 제출함 스프레드시트 `개선요청` 탭.
  `FB_HEADER=[등록일시,작성자,분류,내용,상태,처리메모,처리일시,처리자]`
- 분류 3종(🐞 오류 / 💡 개선 / ❓ 문의), 상태 4종(접수·진행중·완료·보류).
- 등록·상태변경·삭제 **전원 개방**(다른 기능과 같은 원칙). 수정·삭제 전
  등록일시를 재확인해 행 밀림을 감지(`RowMismatch`).
- ⚠️ **완료 목록을 바깥 expander로 감싸지 말 것** — 항목마다 expander를 쓰므로
  2단 중첩이 되어 앱이 죽는다(제목만 두고 나열).

## 요양시설 스튜디오 (`care_studio/`) — 별개 제품, 2026-08 중단

**이 레포 안에 있지만 Dolbom Studio 와 다른 앱이다.** 과장님 지시로 시작한
주간보호센터·소형 요양원용 돌봄 업무 앱. 사용자가 우리 팀이 아니라 요양보호사다.

- 실행·구조·근거·한계: `care_studio/README.md`
- **중단 상태와 재개 순서: `care_studio/멈춤_재개안내.md`** ← 다시 시작하면 이것부터
- 데이터는 구글시트가 아니라 **로컬 JSON**(`care_studio/data/`, git 제외).
  이용자 건강정보라 이 PC 밖으로 내보내지 않는다.
- 항목을 임의로 만들지 않았다 — 시행규칙 별지 제15호서식(급여제공기록지),
  제11호의4서식(급여제공계획서), 2026 재가급여 평가매뉴얼에서 옮겼다.
- 수집 자료 200MB 는 `시장조사_주간보호센터/`(git 제외, 이 PC에만 있음).

## 배포 (Streamlit Cloud)

- main 브랜치에 push → 자동 재배포 (1~2분)
- 빌드 실패 시: share.streamlit.io → My apps → 앱 → "..." → Manage app → 로그 확인
- 패키지 추가: `app/requirements.txt`에 추가 후 push

## 비밀 파일 (절대 깃허브에 올리지 말 것)

- `app/.streamlit/secrets.toml` — 구글 서비스 계정 키 (Streamlit Cloud에도 같은 내용 등록되어 있음)
- `app/service_account.json` — 동일한 서비스 계정 키 (JSON 원본)

두 파일은 `.gitignore`에 등록되어 있어서 `git status` 에 잡히지 않음. 인계 시 **별도 안전한 경로**(USB, 암호화 메일, 1Password 등)로 전달.

## 외부 리소스 URL

| 항목 | URL |
|------|-----|
| 앱 (배포본) | https://dolbom-studio.streamlit.app |
| GitHub 레포 | https://github.com/carerobot0001-pixel/carerobot-weekly-report |
| 구글시트 | https://docs.google.com/spreadsheets/d/1VX-t21tTlXyGPhxksgcoZ0t9js3ABPn_vpiWi_fJcRg/edit |
| FAQ·관리대장 시트 | URL 비공개 (위 "스마트돌봄스페이스 페이지" 참고 — secrets에 ID 저장) |
| Streamlit Cloud 대시보드 | https://share.streamlit.io |
| Google Cloud Console | https://console.cloud.google.com (프로젝트 ID: `molten-guide-469800-e0`) |

## 문제 해결

- **"Zzzz — This app has gone to sleep" 화면** → 무료 호스팅이라 한동안 방문자가 없으면 절전됨.
  파란 **"Yes, get this app back up!"** 버튼을 누르면 1~2분 뒤 정상 접속 (팀원 누구나 가능, 로그인 불필요).
  main에 push해서 재배포해도 깨어남
- **"You do not have access to this app"** → 레포가 비공개로 바뀌면 앱도 비공개. GitHub Settings → Public 유지
- **"템플릿 HWPX를 선택하거나 업로드해주세요"** → 레포 루트에 `돌봄로봇_업무보고*.hwpx` 파일이 있는지 확인
- **"서비스 계정 인증 실패"** → Streamlit Cloud Secrets에 `secrets.toml` 전체 내용 붙여넣었는지 확인
- **"시트를 열 수 없음"** → 구글시트의 공유 설정에 서비스 계정 이메일(`streamlit-bot@molten-guide-469800-e0.iam.gserviceaccount.com`)이 편집자로 있는지 확인
- **스마트돌봄스페이스 탭에 "시트에 접근할 수 없습니다"** → 해당 구글시트(FAQ 또는 관리대장)에 서비스 계정 이메일이 편집자로 공유됐는지 확인
- **스마트돌봄스페이스 탭에 "시트 ID가 아직 설정되지 않았습니다"** → Streamlit Cloud Secrets에 `[smart_space]` 섹션이 빠진 것. 로컬 `app/.streamlit/secrets.toml`의 같은 섹션을 복사해 붙여넣기
- **HWPX가 한글에서 "파일 손상" 에러로 안 열림** → `TROUBLESHOOTING.md` 의 "1. HWPX 생성본이 한글에서..." 섹션 참고. 탭 문자·자간·flag_bits 등 과거 범인 5종과 재발 시 체크리스트가 정리되어 있음
- **기타 과거 비자명 오류 기록** → `TROUBLESHOOTING.md` 전체

## 세팅 초기 이력

2026년 4월 ~ 김건양 연구원이 Claude Code로 설계·구현. 자세한 초기 설치 절차는 `app/SETUP.md` 참고.
